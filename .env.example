# Northstar frontend environment variables

# API base URL. Defaults to /api (uses the Vite dev proxy). Set to an absolute URL
# only when the backend is served from a different origin without the proxy.
VITE_API_BASE=/api

# Default IANA timezone for the attendance widget.
VITE_DEFAULT_TZ=Asia/Tokyo
